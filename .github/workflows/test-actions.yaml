name: "test actions"
run-name: "test actions"
on:
  push:
    branches: [ release-* ]
    paths:     
     - 'dir1/**'
  create:
jobs:
  echo:
    if: ${{ startsWith(github.ref, 'release-') }}
    runs-on: ubuntu-latest
    steps:
     - name: Hello World
       run: |
        echo "Hello World"
